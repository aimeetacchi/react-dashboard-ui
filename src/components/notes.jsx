import React, { Component } from 'react';

export default class notes extends Component {
    render() {
        return (
            <div>
                Notes component
            </div>
        )
    }
}
